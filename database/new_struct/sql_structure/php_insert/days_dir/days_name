
// import days

$days_id = array();
$days_name = array();

$arr_days = array(
  array("id", $days_id),
  array("name", $days_name),
);

$sql_days = "SELECT id, name FROM days WHERE name = ' . $link_value . ';

$result_days = import_arr($sql_days, $arr_days);

$days_id = $result_days[0][1];
$days_name = $result_days[1][1];
